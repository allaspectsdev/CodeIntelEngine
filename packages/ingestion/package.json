{
  "name": "@codeintel/ingestion",
  "version": "0.1.0",
  "description": "Streaming incremental parsing and graph construction pipeline",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@codeintel/core": "*",
    "tree-sitter": "^0.22.4",
    "tree-sitter-typescript": "^0.23.2",
    "tree-sitter-javascript": "^0.23.1",
    "tree-sitter-python": "^0.23.6",
    "tree-sitter-go": "^0.23.4",
    "tree-sitter-rust": "^0.23.2",
    "tree-sitter-java": "^0.23.5",
    "chokidar": "^4.0.0",
    "glob": "^11.0.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.7.0"
  }
}
